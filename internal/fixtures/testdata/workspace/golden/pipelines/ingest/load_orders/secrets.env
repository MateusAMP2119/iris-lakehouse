# Golden sample workspace: secrets.env for load_orders env_file demo (spec section 3).
# Loaded fresh at every run; never stored in meta. Values here are lower precedence
# than host env and the declared `env:` map. A minimal/empty file is valid.
# Example (commented so no side effects):
# FOO=bar
