---
status: todo
epic: E16
created: 2026-07-12
tags:
  - iris
  - task
---

# E16.2 Pipeline catalog registry and starter entries

Epic: [[Iris Epics]], E16 Install Ceremony and Pipeline Catalog
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Turn the single embedded sample into the catalog registry. Rename
`internal/cli/quickstartdata/` → `catalogdata/` with the per-entry layout:
`catalog.yaml` ordered index (entry 1 = `hello_iris`, the default pick), one folder per
entry holding `entry.yaml` (id matching folder, display name, one-line pitch,
description, run_note, showcase table+pk for the provenance finale) beside a
`workspace/` subtree mirroring exactly what materializes. Move hello_iris verbatim.
Add two starter entries per the design doc: `system_snapshot` (POSIX uname/df facts
upserted into `demo.machine_facts`, fixed fact pks, degrade-to-'unavailable') and
`word_frequency` (embedded 1891 Dickinson poem word counts computed wholly in Postgres
into `demo.word_counts`, deterministic, showcase pk `hope`). Starter constraints:
POSIX sh + psql over `IRIS_DB_URL` (PATH first, else upward walk to
`.iris/pg/bin/psql`), fixed pks + `ON CONFLICT DO UPDATE`, no lane, no other hard
dependency. Land `internal/cli/catalog.go`: `loadCatalog` (fresh per call),
`catalogEntryByID` (error names available ids), `materializeCatalogEntry` (per-entry
walk root; reuse the never-clobber temp+link writer byte-for-byte). The tour still
selects hello_iris unconditionally at the end of this task — steps parameterization
lands (`quickstartPipelineSteps(entry)`), the shop arrives in E16.3.

Spec text and contract rows landed in E16.1; this task claims only.

## Depends on

[[E16.1 Act structure and workspace prompt]] (act framework, spec delta, contract rows).

## Contracts (1 new + 2 re-claimed)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S08/quickstart-catalog-entries-valid` | Every embedded entry parses through the real declare loaders; entry.yaml id matches folder, fields non-empty, showcase.table among declared writes; catalog.yaml lists exactly the entry folders, ids unique, hello_iris first. | unit |
| `S08/quickstart-sample-valid-declaration` | Re-claimed at the new path (catalogdata/hello_iris/workspace/…), generalized over all entries. | unit |
| `S08/quickstart-sample-materialize-never-clobber` | Re-claimed via materializeCatalogEntry("hello_iris", …): write-if-absent, byte-compare, keep+warn. | integration |

## Done when

- [ ] Failing tests first; existing test bodies generalized with the already-landed spec delta, never weakened
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
