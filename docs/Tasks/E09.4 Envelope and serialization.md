---
status: todo
epic: E09
created: 2026-07-04
tags:
  - iris
  - task
---

# E09.4 Envelope and serialization

Epic: [[Iris Epics]], E09 Read API, Endpoints and PATs
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Deliver the response-side half of the wire contract: the data/page success envelope, the shared error envelope with its closed code set, and per-column-type JSON serialization rules. Pure unit work against compiled shapes that fixes the byte-level output every route task then serves.

## Depends on

Epics E02, E03, E04; builds on [[E09.3 Param grammar and paging]].

## Contracts (3 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S07/column-type-serialization` | Values serialize per column type: int/bigint/smallint/double as JSON numbers, numeric as string, bool as JSON boolean, text/varchar/uuid as strings, timestamptz/timestamp/date/time as RFC 3339 strings, json/jsonb inline, bytea as base64, and recorded_at audit strings opaque, never interpreted for ordering. | unit |
| `S07/error-envelope-closed-codes` | Errors reuse the same envelope with an error object whose code comes only from the closed set {bad_param, unauthorized, forbidden, not_found, method_not_allowed, internal}. | unit |
| `S07/response-envelope` | Successful responses use the { "data": [...], "page": { "next_after": <key\|null>, "limit": <n> } } envelope with rows mirroring source columns. | unit |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
