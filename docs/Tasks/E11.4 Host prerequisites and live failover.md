---
status: todo
epic: E11
created: 2026-07-04
tags:
  - iris
  - task
---

# E11.4 Host prerequisites and live failover

Epic: [[Iris Epics]], E11 High Availability and Failover
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Close the epic with per-host prerequisites and the live-daemon failover leg: a candidate refuses to start on a host lacking the workspace tree, and a promoted leader dispatches built runs from its own objects_path. The real conformance leg (two candidates sharing one meta, kill the leader, standby takes over) rides E13 step 9 per the epic's cutting notes.

## Depends on

Epics E02, E05; builds on [[E11.3 Promotion and self demotion]]; the conformance rows ride E13 step 9.

## Contracts (4 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S15/candidate-requires-workspace-tree` | A daemon candidate started on a host lacking the workspace tree the leader dispatches from (pipeline folders, dev source, env_files) refuses to start. | integration |
| `S15/failover-leader-own-objects-path` | A leader promoted by failover dispatches built runs using artifact bytes resolved from its own objects_path. | integration |
| `S15/failover-standby-takes-over` | With two real daemon candidates sharing one meta, killing the leader causes the standby to acquire the advisory lock and take over as the dispatching leader. | conformance |
| `S16/failover-real-leader-kill` | With two real daemon candidates sharing one meta, killing the leader results in the standby taking over. | conformance |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
