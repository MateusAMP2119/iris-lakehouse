---
status: todo
epic: E13
created: 2026-07-04
tags:
  - iris
  - task
---

# E13.8 Failover and unattended closure

Epic: [[Iris Epics]], E13 Golden Sample and Acceptance
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Close the epic with the live-daemon scenario steps and the final gate: killing the leader lets a second candidate on the same meta take over, reconcile, and resume lanes; a mutation against a standby is rejected with leader guidance and exit code 6; and the golden sample passes the full 11-step acceptance scenario end-to-end unattended. This is the last task, done when every earlier step is green without supervision.

## Depends on

All other epics (E13 is the spine that proves them); requires every earlier E13 task, closing after [[E13.7 Endpoint reads and grants]].

## Contracts (3 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S13/failover-standby-takeover` | When the leader is killed, a second candidate on the same meta acquires the lock, reconciles (orphan dead-letters marked stopped), reports itself leader, and resumes lanes. | conformance |
| `S13/standby-mutation-exit-6` | A mutation attempted against a standby is rejected with leader guidance and exit code 6. | conformance |
| `S13/scenario-passes-unattended` | The golden sample passes the full 11-step acceptance scenario end-to-end unattended. | conformance |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
