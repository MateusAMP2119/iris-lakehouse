---
status: todo
epic: E07
created: 2026-07-04
tags:
  - iris
  - task
---

# E07.2 Snapshot pin

Epic: [[Iris Epics]], E07 Provenance, Journal Lifecycle and Object Store
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Record the snapshot pin around each run: the data database LSN and journal high id at dispatch, and the journal high id again at terminal transition. Copy all three pin values into run_summaries so the pin stays queryable after the run row is pruned, which the ==provenance walk's== summary fallback relies on.

## Depends on

Epics E05, E06.

## Contracts (2 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S14/pin-recorded-dispatch-terminal` | At dispatch a run records the data database LSN as snapshot_lsn and the journal high id as journal_floor, and at terminal transition records the journal high id again as journal_ceiling. | integration |
| `S14/pin-survives-pruning` | run_summaries copies all three pin values (snapshot_lsn, journal_floor, journal_ceiling) so the pin remains queryable after the run row is pruned. | unit |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
