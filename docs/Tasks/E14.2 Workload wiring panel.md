---
status: todo
epic: E14
created: 2026-07-04
tags:
  - iris
  - task
---

# E14.2 Workload wiring panel

Epic: [[Iris Epics]], E14 Graph Views and Triage Surface
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

The iris workload show wiring panel over the two-graphs doctrine. It renders the standing wiring (lanes with composer walk, artifact and data mode, run tips, per-edge live gate state) as a panel, never a commit graph. A named pipeline zooms to that pipeline's neighborhood. This is the wiring view (what may run), kept distinct from the lineage rendering.

## Depends on

Epics E05, E07, E09. Builds before E13, the acceptance spine. Plus [[E14.1 Ref grammar and triage shows]] (the per-edge live gate state reuses the gate ledger verdict).

## Contracts (1 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S08/workload-show-wiring-panel` | iris workload show renders the standing wiring (lanes with composer walk, artifact and data mode, run tips, per-edge live gate state) as a panel, never a commit graph, and a named pipeline zooms to that pipeline's neighborhood. | integration |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
