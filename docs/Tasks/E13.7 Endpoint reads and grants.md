---
status: todo
epic: E13
created: 2026-07-04
tags:
  - iris
  - task
---

# E13.7 Endpoint reads and grants

Epic: [[Iris Epics]], E13 Golden Sample and Acceptance
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Prove the read surface and its enforcement on the golden sample's declared endpoint: after iris endpoint apply publishes orders_by_customer, a data PAT scoped to it reads via /q/ and the raw surface, and a read touching an ungranted field fails at Postgres itself, showing grants are physically enforced rather than checked in application code.

## Depends on

All other epics as they land (E13 is the spine that proves them); the endpoint is declared in [[E13.1 Golden workspace fixture]].

## Contracts (2 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S13/data-pat-reads-endpoint` | After iris endpoint apply publishes the endpoint, a data PAT minted with --endpoint orders_by_customer reads it via /q/ and via the raw surface. | conformance |
| `S13/ungranted-field-fails-postgres` | A read touching an ungranted field fails at Postgres itself rather than in application code. | conformance |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
