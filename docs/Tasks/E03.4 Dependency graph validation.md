---
status: todo
epic: E03
created: 2026-07-04
tags:
  - iris
  - task
---

# E03.4 Dependency graph validation

Epic: [[Iris Epics]], E03 Declarations, Schemas and Apply
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Implement the depends_on graph rules as pure logic: upstream-first registration (references must already be registered), acyclicity over the registered graph plus the new declaration with the offending chain named in errors, and acceptance of cross-lane edges. These checks run on every apply and are unit-testable before any daemon exists.

## Depends on

Epic E01; builds on [[E03.1 Declaration parsing and discovery]].

## Contracts (5 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S03/depends-on-cycle-rejected` | Apply rejects a depends_on graph containing a cycle (including self-reference) and names the offending chain in the error. | unit |
| `S03/unregistered-ref-rejected` | Apply rejects a depends_on reference to a pipeline not already registered (upstream-first, single-file), failing immediately rather than deferring resolution. | unit |
| `S04/dependencies-cross-lane-ok` | A depends_on edge between pipelines in different lanes is accepted as valid. | unit |
| `S06.3/apply-rejects-cycles` | Every apply checks acyclicity over the registered graph plus the new declaration and rejects a cycle by naming the chain. | unit |
| `S06.3/apply-upstream-first` | Apply rejects a declaration whose depends_on names an unregistered pipeline, and the rejection names the missing pipeline. | unit |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
