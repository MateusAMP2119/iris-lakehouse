---
status: todo
epic: E04
created: 2026-07-04
tags:
  - iris
  - task
---

# E04.1 Access declaration validation

Epic: [[Iris Epics]], E04 Roles, Grants and Credentials
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Deliver the apply-time validation and semantics of pipeline reads/writes access declarations, the pure unit-heavy foundation the rest of the grants machinery builds on. Every entry must name a dotted schema.table with an explicit fields list, anything targeting the engine-reserved public schema is rejected (both public.* entries and a public schema folder), and reads/writes are established as access-only declarations that never create ordering or dependency edges.

## Depends on

Epics E02, E03. Nothing beyond the epic's own dependencies.

## Contracts (4 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S03/access-entry-table-fields-required` | Each reads/writes entry must name a dotted schema.table plus an explicit fields list; an entry omitting fields is rejected with no implicit all-columns fallback. | unit |
| `S03/public-reads-writes-rejected` | Apply rejects any reads/writes entry targeting public.*. | unit |
| `S03/public-schema-folder-rejected` | Apply rejects a public schema folder under schemas/ (public is engine-reserved). | unit |
| `S03/reads-writes-no-ordering` | reads/writes govern access only and are not exclusive: overlapping entries between pipelines are accepted and create no dependency edges or run ordering, which derives only from lanes and depends_on. | unit |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
