---
status: todo
epic: E01
created: 2026-07-04
tags:
  - iris
  - task
---

# E01.3 Config precedence

Epic: [[Iris Epics]], E01 Repo Skeleton, CLI Frame and Config
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Implement configuration resolution with strict precedence: command flags over IRIS_* environment variables over iris.toml over built-in defaults. Recognize every documented environment variable, keep iris.toml scoped to engine/connection settings only (never a project manifest), and make the zero-config path default to the local socket with managed Postgres. All of this is unit-testable resolution logic layered on the flag surface from the cobra tree.

## Depends on

Epic E00; builds on [[E01.2 Cobra tree and exit codes]].

## Contracts (4 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S08/config-precedence-order` | Configuration resolves in strict precedence: command flags override IRIS_* environment variables, which override iris.toml values, which override built-in defaults. | unit |
| `S08/documented-env-vars-recognized` | Each documented environment variable (IRIS_SOCKET, IRIS_HOST, IRIS_TOKEN, IRIS_PG_DSN, IRIS_RETAIN, IRIS_JOURNAL_PARTITION_ROWS, IRIS_OBJECTS_PATH) is recognized and supplies the value for its corresponding setting. | unit |
| `S08/iris-toml-engine-settings-only` | iris.toml is limited to engine/connection settings and is never treated as a project manifest (project-level keys are not honored). | unit |
| `S08/zero-config-defaults` | With no flags, env vars, or iris.toml present, the CLI defaults to the local socket and the engine defaults to managed Postgres. | unit |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
