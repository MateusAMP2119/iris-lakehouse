---
status: todo
epic: E09
created: 2026-07-04
tags:
  - iris
  - task
---

# E09.10 Read parity closure

Epic: [[Iris Epics]], E09 Read API, Endpoints and PATs
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Close the epic against a live daemon: prove CLI readouts and API routes serve the same curated views (including exact render parity between GET responses and CLI output), serve the provenance lineage route under the read scope without row images and matching the CLI walk, and verify a read-scoped PAT can fetch /stats. This is the cross-surface conformance pass that pins the whole read surface together.

## Depends on

Epics E02, E03, E04; builds on [[E09.8 Q and data routes]].

## Contracts (5 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S07/cli-api-same-views` | CLI readouts print the same curated views the corresponding API routes serve. | conformance |
| `S07/provenance-route-lineage-only` | ==GET /provenance/{schema}/{table}/{pk} returns lineage under the read scope alone, carrying what data provenance prints (the full layer list with per-stamp disposition), and never includes row images.== | integration |
| `S10/api-cli-read-render-parity` | For every read surface, the HTTP api handler returns exactly the same content the corresponding CLI read command renders, proven by comparing GET responses from an in-process daemon against CLI output. | integration |
| `S11/stats-read-pat-access` | A monitor holding a read-scoped PAT can successfully fetch GET /stats. | integration |
| `S14/provenance-http-endpoint` | GET /provenance/{schema}/{table}/{pk} serves the same provenance walk result over the HTTP API as the CLI walk returns. | integration |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
