---
status: todo
epic: E13
created: 2026-07-04
tags:
  - iris
  - task
---

# E13.6 Data provenance lineage

Epic: [[Iris Epics]], E13 Golden Sample and Acceptance
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Prove ==iris data provenance== answers across the whole retention story on the golden sample: full lineage (writing run, pipeline, hashes, consumed upstream) in one query while history is live, still answering from the archival summary after pruning past the writing run, and still answering stamps from a partition that was archived, exported, and dropped from Postgres.

## Depends on

All other epics as they land (E13 is the spine that proves them); builds on sealed and exported partitions from [[E13.5 Sealing and archival]].

## Contracts (3 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| ==`S13/data-provenance-full-lineage`== | ==iris data provenance analytics.orders <pk> answers with the writing run, pipeline, hashes, and consumed upstream in one query.== | conformance |
| ==`S13/data-provenance-after-prune`== | ==After pruning past the writing run, iris data provenance still answers for the row from the archival summary.== | conformance |
| ==`S13/archived-partition-provenance-answers`== | ==iris data provenance still answers stamps from an archived partition that was exported and dropped from Postgres.== | conformance |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
