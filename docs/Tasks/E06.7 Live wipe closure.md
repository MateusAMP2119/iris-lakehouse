---
status: todo
epic: E06
created: 2026-07-04
tags:
  - iris
  - task
---

# E06.7 Live wipe closure

Epic: [[Iris Epics]], E06 Write Capture, Wipe and Promotion
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Close the epic against a real database: ==`iris workload wipe [<pipeline>]`== runs in a single data-database transaction, exactly reverts in-scope disposable rows without ever clobbering permanent writes, and retains all journal rows. Also carries the data-durability overhead gate; the 1.25x bulk-capture acceptance scenario itself lands in E13, this task owns the contract row.

## Depends on

Epics E03, E05. Builds on [[E06.5 Wipe replay and conflicts]] and [[E06.6 Promotion]].

## Contracts (4 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S05/wipe-atomic-transaction` | Wipe executes in a single data-database transaction (journal and tables co-reside) so a mid-wipe failure leaves no partial wipe applied. | conformance |
| `S05/wipe-never-clobbers-permanent` | Against a real database, wipe exactly reverts in-scope disposable rows while permanent writes are never clobbered or silently dropped. | conformance |
| `S12/wipe-reverts-unpromoted-keeps-journal` | ==iris workload wipe reverts un-promoted disposable data while retaining all journal rows.== | conformance |
| `S14/capture-overhead-budget` | A 10M-row promoted bulk insert completes within 1.25x of the capture-less baseline, gated by the acceptance scenario. | conformance |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
