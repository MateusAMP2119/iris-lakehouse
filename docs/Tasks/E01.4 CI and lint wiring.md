---
status: todo
epic: E01
created: 2026-07-04
tags:
  - iris
  - task
---

# E01.4 CI and lint wiring

Epic: [[Iris Epics]], E01 Repo Skeleton, CLI Frame and Config
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Wire CI and lint for the module: a Go 1.25/1.26 build matrix, a cgo-disabled build producing a portable statically linked binary, and the repo's code style conventions enforced via gofmt/goimports/golangci-lint. This closes the epic with the integration-tier build checks and the exempt style convention, running against the layout, CLI frame, and config from the earlier tasks.

## Depends on

Epic E00; builds on [[E01.1 Module and package layout]].

## Contracts (2 testable, 1 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S09/cgo-free-static-binary` | Building the engine with cgo disabled succeeds and yields a portable statically linked binary with no dynamic library dependencies. | integration |
| `S09/go-version-floor-and-target` | The engine module compiles successfully under both the floor toolchain Go 1.25 and the target toolchain Go 1.26. | integration |
| `S10/code-style-conventions` | Code style follows plain idiomatic Go with gofmt/goimports/golangci-lint in CI, %w error wrapping without cross-package panics, context threading through blocking calls, slog-only logging, no mutable package globals, table-driven tests, and doc comments on exported identifiers (repo convention, non-behavioral). | exempt |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
