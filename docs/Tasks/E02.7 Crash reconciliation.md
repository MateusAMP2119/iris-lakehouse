---
status: todo
epic: E02
created: 2026-07-04
tags:
  - iris
  - task
---

# E02.7 Crash reconciliation

Epic: [[Iris Epics]], E02 Engine Install, Daemon and Leadership
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

Implement startup reconciliation, identical on cold start and failover, completed by the leader before any lane dispatch: in-flight runs are dead-lettered with a daemon-terminated reason, queued never-started runs are deleted for the next pass to recreate, surviving process groups are best-effort SIGKILLed on same-host restart, and the journal is never touched so crashed partials remain revertible.

## Depends on

Epic E01; builds on [[E02.6 Leader election single writer]].

## Contracts (5 testable, 0 exempt)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S02/inflight-runs-deadlettered` | Reconciliation dead-letters leftover running runs with the reason that the daemon terminated while the run was in flight. | unit |
| `S02/queued-runs-deleted` | Reconciliation deletes queued never-started runs rather than dead-lettering them, leaving the next pass to recreate them. | unit |
| `S02/reconcile-before-dispatch` | The leader completes startup reconciliation before dispatching any lane, using identical logic on cold start and failover. | integration |
| `S02/reconcile-no-journal-touch` | Reconciliation performs no journal step: capture rows stay where triggers wrote them, crashed disposable-run partials remain revertible, and nothing auto-replays. | unit |
| `S02/samehost-restart-kills` | On same-host restart, reconciliation best-effort SIGKILLs surviving process groups from recorded handles before disposing of their runs. | integration |

## Done when

- [ ] Contract rows present in spec/contracts.yaml for every id above
- [ ] Failing tests written at each contract's tier (exempt rows marked exempt in the manifest, no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] Traceability gate passes with no other contract broken
